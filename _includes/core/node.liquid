{%-  assign title = '('
       | append: order
       | append: ')'
       | append: tag                        -%}

{%-  assign stage = '['
       | append: num
       | append: ';'
       | append: order
       | append: ';'
       | append: width
       | append: ']'                        -%}

{%-  assign details = '['
       | append: weight
       | append: ';'
       | append: expanse
       | append: ';'
       | append: property
       | append: ']('
       | append: keys_size
       | append: ')'                        -%}

{%-  assign list = '<li data-id="'
       | append: stage
       | append: '"'                       -%}

{%-  assign node = list
       | append: '><a href="'
       | append: baseurl
       | append: '" id='
       | append: width
       | append: ' aria-details="'
       | append: details
       | append: '">'
       | append: title
       | append: '</a></li>'                -%}

{%-  capture nodes                          -%}
{{     nodes                                 }}
{%     include tabs.liquid size=14 %}{{ node }}
{%-  endcapture                             -%}

